/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import React, { Component } from 'react';
import {
    AppRegistry
} from 'react-native';

let root = require('./src/Root');

AppRegistry.registerComponent('RNBuy', () => root);
